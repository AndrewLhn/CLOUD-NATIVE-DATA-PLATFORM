name: cloud_native_data_platform
version: "1.0.0"
config-version: 2

profile: cloud_native_data_platform

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages", "logs"]

models:
  cloud_native_data_platform:
    staging:
      +materialized: table
      +schema: analytics
    marts:
      +materialized: table
      +schema: analytics
